import 'package:flutter/material.dart';

/// Centralised earthy color palette for the Oasis Spa app.
/// Import this file anywhere you need consistent branding colors.
class SpaColors {
  SpaColors._(); // prevent instantiation

  /// Background – soft sand / cream
  static const Color sand = Color(0xFFF4EFE1);

  /// Primary accent – rich terracotta brown
  static const Color terracotta = Color(0xFF6B4E31);

  /// Warm beige card backgrounds
  static const Color warmBeige = Color(0xFFF5EDE3);

  /// Muted olive for subtle accents
  static const Color olive = Color(0xFF8A9A5B);

  /// Deep brown for body text
  static const Color deepBrown = Color(0xFF3E2723);
}
